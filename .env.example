# Django
SECRET_KEY=change-me-to-a-random-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# PostgreSQL (matches docker-compose service name "db")
DB_NAME=roupadegala
DB_USER=postgres
DB_PASSWORD=changeme
DB_HOST=db
DB_PORT=5432

# Admin bootstrap (used by initial data migration if present)
ADMIN_PASSWORD=changeme

# JWT (SimpleJWT)
# Access token: 7 days (default), Refresh token: 30 days (default)
# Override here if needed — see roupadegala/settings.py
